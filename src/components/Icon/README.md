Basic Icon:

```
<div>
  <Icon glyph="logo" />
  <Icon glyph="done" />
  <Icon glyph="settings" />
  <Icon glyph="call" />
  <Icon glyph="clear" />
  <Icon glyph="place" />
  <Icon glyph="phone" />
  <Icon glyph="apple" />
  <Icon glyph="more" />
</div>
```

Themed Icons:

```
<div>
  <Icon glyph="logo" theme="primary" />
  <Icon glyph="done" theme="success" />
  <Icon glyph="settings" theme="danger" />
  <Icon glyph="call" theme="info" />
  <Icon glyph="clear" theme="warning" />
</div>
```

Inverted Icon:

```
<div>
  <Icon glyph="logo" theme="primary" inverted />
  <Icon glyph="done" theme="success" inverted />
  <Icon glyph="settings" theme="danger" inverted />
  <Icon glyph="call" theme="info" inverted />
  <Icon glyph="clear" theme="warning" inverted />
</div>
```

Clickable Icon:

```
<div>
  <Icon glyph="logo" theme="primary" inverted onClick={() => {}} />
  <Icon glyph="done" theme="success" onClick={() => {}} />
  <Icon glyph="settings" theme="danger" inverted onClick={() => {}} />
  <Icon glyph="call" theme="info" onClick={() => {}} />
  <Icon glyph="clear" theme="warning" inverted onClick={() => {}} />
</div>
```
